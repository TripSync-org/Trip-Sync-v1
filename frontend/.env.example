# Vite frontend — copy to frontend/.env (repo root .env may also define VITE_* for monorepos)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
# Optional: absolute API URL for voice REST (otherwise use Vite proxy /api → localhost:3000)
# VITE_API_URL=https://your-backend.vercel.app
